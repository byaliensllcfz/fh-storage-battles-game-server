apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: <NAME>-<URL_VERSION>
  name: <NAME>-deployment-<URL_VERSION>
spec:
  replicas: 1
  selector:
    matchLabels:
      app: <NAME>-<URL_VERSION>
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: <NAME>-<URL_VERSION>
    spec:
      containers:
        - image: gcr.io/forhumans-gameservices-dev/<NAME>:<BUILD_VERSION>
          livenessProbe:
            failureThreshold: 5
            httpGet:
              path: /liveness-check
              port: 2567
              scheme: HTTP
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 1
          name: <NAME>-<URL_VERSION>
          ports:
            - containerPort: 2567
              protocol: TCP
          readinessProbe:
            failureThreshold: 30
            httpGet:
              path: /readiness-check
              port: 2567
              scheme: HTTP
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 1
          volumeMounts:
            - name: google-cloud-key
              mountPath: /var/secrets/google
            - name: config-files
              mountPath: /app/configuration
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
      volumes:
        - name: google-cloud-key
          secret:
            defaultMode: 420
            secretName: tpserver-api-key
        - name: config-files
          configMap:
            name: config-<NAME>-<URL_VERSION>
