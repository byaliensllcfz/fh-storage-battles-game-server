service: servicename-v1
runtime: nodejs
env: flex

# Prod env settings
automatic_scaling:
  min_num_instances: 1
  max_num_instances: 20
  cool_down_period_sec: 120 # default value
  cpu_utilization:
    target_utilization: 0.7